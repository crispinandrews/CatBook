<div class="inside_left">
  <% if current_user %>
    <p>Hey there. Welcome to CatHub! Thanks for signing up!<p>
    <p>Now go make some friends(or enemies :p) and have some FUN!!!</p>
    <h3>Cats in Da Hub</h3>
    <% unless @users.nil? %>
     <ul>
     <% @users.each do |user| %>
       <% if current_user %>
         <% unless user == current_user %>
           <% unless current_user.relations.include?(user) || current_user.inverse_relations.include?(user)  %>
                 <li>
                   <%= image_tag user.image.url(:thumb) %>
                   <strong><%= user.petname %></strong>
                   <%= link_to "Add Friend", relationships_path(relation_id: user), method: :post %>
                 </li>
                 <% end %>
               <% end %>
         <% end %>
       <% end %>
     </ul>
   <% end %>
   <% else %>
   <p>Hey there. Welcome to CatHub! Sign up or sign in!<p>
 <% end %>
</div>
